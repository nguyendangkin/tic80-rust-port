[package]
name = "tic80-rust"
version = "0.1.0"
edition = "2021"
description = "TIC-80 fantasy computer — Rust port"

[features]
default = ["sdl2"]
sdl2 = ["dep:sdl2"]

[dependencies]
flate2 = "1.0"
serde_json = "1.0"
sdl2 = { version = "0.38", optional = true }
